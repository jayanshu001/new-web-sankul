/node_modules
/dist
/package-lock.json
# /yarn.lock
/.htaccess
/.yarn
# Yarn Berry local config (repo uses Yarn Classic) — machine-specific, not committed
/.yarnrc.yml
/logs
/.env

# Secrets — never commit
/secrets/

# Python artifacts (utility scripts)
__pycache__/
*.pyc

# macOS
.DS_Store

# Stray uploaded player scripts at repo root
/*-player-script.js

# Migration API test mock JWTs (test-only, short-lived)
docs/migration/api-tests/.auth.json

# Per-module API-doc capture sidecar (regenerated each run)
docs/migration/api-tests/*/.api-doc-calls.json

# Local-only tooling/docs — kept on disk, not tracked in git
/.claude/
/docs/admin
/docs/audit
/docs/backend
/docs/client
/docs/frontend
/docs/notifications

# Throwaway end-to-end/diagnostic probes (kept local, never pushed)
scripts/_tmp_*.ts

graphify-out/
.graphify/cache/

# Reference cache docs copied from another project — NOT this project's source
# of truth. Authoritative caching doc is docs/CACHING.md. Kept local for
# reference, ignored so it doesn't clutter real diffs.
/cache/
/loadtest